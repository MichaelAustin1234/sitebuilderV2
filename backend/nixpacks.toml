[phases.setup]
nixPkgs = ["php82", "php82Packages.composer", "nodejs"]

[phases.build]
cmds = [
  "composer install --no-dev --optimize-autoloader"
]

[start]
cmd = "php artisan storage:link; php artisan migrate --force --seed; php artisan serve --host 0.0.0.0 --port $PORT"
